{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // Chrome-only globals that the DOM lib doesn't know about (gBrowser,
    // Services, window.zenTidyTabs) are declared in src/chrome-globals.d.ts.
    "types": ["node"]
  },
  "include": [
    "src",
    "specs",
    "manual",
    "playwright.config.ts",
    "manual.config.ts"
  ]
}
